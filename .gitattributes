# The source manifest hashes published bytes. Keep checkout bytes identical on every OS.
* -text

# Future executable scripts must use LF line endings.
*.sh text eol=lf
*.bash text eol=lf
*.command text eol=lf
